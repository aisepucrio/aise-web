{
  "name": "monorepo-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:editor": "npm run dev --workspace apps/editor",
    "dev:main": "npm run dev --workspace apps/main"
  }
}
